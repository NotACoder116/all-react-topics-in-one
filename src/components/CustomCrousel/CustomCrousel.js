import React from 'react'

const CustomCrousel = ({ images = [], imageLimit = images.length, customPrevButton, customNextButton, imagePerSlide = 1 }) => {
  return (
    <div>CustomCrousel</div>
  )
}

export default CustomCrousel